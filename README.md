# Search Functionality (PHP + MySQL)

## Features
- Search bar for filtering posts
- Prepared statements to prevent SQL injection
- Partial matching using LIKE
- Shows dynamic search results

## How to Use
1. Import the SQL file (optional).
2. Update database credentials in `db.php`.
3. Run: http://localhost/search-feature/index.php
4. Try searching for keywords in titles or content.
